import 'package:flutter/material.dart';

class TrackTile extends StatelessWidget {
  const TrackTile({super.key});

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text('Track #1'),
    );
  }
}